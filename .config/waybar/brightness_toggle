echo $(echo "$(light) == 50" | bc -l)
if  [ $(echo "$(light) == 50" | bc -l) -eq 1 ]; then
    light -S 100
elif  [ $(echo "$(light) < 50" | bc -l) -eq 1 ]; then
    echo "50"
    light -S 50
else
    echo "30"
    light -S 30
fi
